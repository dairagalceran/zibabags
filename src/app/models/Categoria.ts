export interface Categoria {
  id: number,
  name: string,
  price: number,
  stock: number,
  image: string,
  clearence: boolean,
  quantity: number,
}

//export permite que la clase pueda ser importada por otras clases
